requests~=2.32.3
spotipy~=2.25.1
yt-dlp~=2025.2.19
ffmpeg~=1.4
ffmpeg-python~=0.2.0

# keys.txt must be formatted as follows (without the comment hashes, obviously)
# (ACRCloud client ID)
# (ACRCloud secret ID)
# (Spotify client ID)
# (Spotify secret ID)